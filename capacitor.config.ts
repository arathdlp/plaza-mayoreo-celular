import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.plazamayoreo.celular',
  appName: 'Plaza Mayoreo del Celular',
  webDir: 'out'
};

export default config;
